//math

let maxVal=Math.max([100,2,33,44,1]);

let minVal=Math.min(100,2,33,44,1);
console.log(maxVal); //Nan
console.log(minVal); //1

let maxValue =Math.max.apply(null,values)
console.log(maxValue);